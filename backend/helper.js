/**emptyOrRows
 * Checks if there is data in a row or not.
 *  @returns rows or empty list
 */
function emptyOrRows(rows) {
  if (!rows) {
    return [];
  }
  return rows;
}
  
module.exports = {
  emptyOrRows
}